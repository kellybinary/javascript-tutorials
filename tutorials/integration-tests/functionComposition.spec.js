const functionComposition = require('./functionComposition.js');

describe.skip('functionComposition unit', () => {
  it('should return true', () => {
    expect(functionComposition()).toBe(true);
  });
});
